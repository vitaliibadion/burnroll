/// Placement names Superwall can later attach a campaign to.
/// Do not add Superwall-designed paywalls to these unless you want Superwall UI
/// instead of the native screens.
enum SuperwallPlacement {
    static let startCleaning = "start_cleaning"
    static let confirmDelete = "confirm_delete"
    static let restorePurchases = "restore_purchases"
}
